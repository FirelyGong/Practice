<Window x:Class="Demo.AsyncTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AsyncTestWindow" Height="500" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Content="Async start" Click="Button_Click"></Button>
            <Button Grid.Column="1" Content="Sync start" Click="Button2_Click"></Button>
        </Grid>
        <Grid Grid.Row="1">
            <ListBox Name="listBox"></ListBox>
        </Grid>
    </Grid>
</Window>
